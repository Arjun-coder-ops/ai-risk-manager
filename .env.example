# Port the Flask backend listens on
PORT=8000

# Optional: only needed to enable the optional LLM explanation layer
# (model/llm_explainer.py). Everything else in this project works without it.
# If unset, the system falls back to a deterministic templated explanation
# built from the same verified signals -- it never blocks or fabricates.
ANTHROPIC_API_KEY=
